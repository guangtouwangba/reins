## 1. Shared content builder

- [ ] 1.1 Create `src/adapters/shared-content.ts` with `SharedContent` interface and `buildSharedContent(constraints, context, config)` function. Extract the common content logic (project summary, constraint grouping by severity, commands section, conventions section) from existing adapters into this module.
  - Files: `src/adapters/shared-content.ts`
  - Tests: Unit test that `buildSharedContent` returns all fields populated from a fixture context. Unit test that `criticalRules` contains only critical-severity constraints. Unit test with empty constraints returns empty arrays.
  - Done when: SharedContent interface defined; builder produces correct output from fixture data; `pnpm typecheck` passes

- [ ] 1.2 Add a `commandsBlock` builder to shared-content that reads `context.commands` and produces a formatted commands section with source annotations (`(declared)`, `(from docs)`, `(may need adjustment)`).
  - Files: `src/adapters/shared-content.ts`
  - Tests: Unit test with a full CommandMap produces all commands listed. Unit test with null commands produces empty block. Unit test that source annotations appear for user/docs sources.
  - Done when: Commands section matches the format from `l0-generator.ts`; existing context generator can delegate to this module later

## 2. Adapter interface redesign

- [ ] 2.1 Define `AdapterDefinition` interface in `src/adapters/base-adapter.ts` alongside the existing `Adapter` interface. Add `AdapterInput`, `AdapterOutput` types. Export `ADAPTER_REGISTRY: AdapterDefinition[]` (initially empty).
  - Files: `src/adapters/base-adapter.ts`
  - Tests: Unit test that `AdapterDefinition` type is assignable with a minimal mock adapter. `pnpm typecheck` passes.
  - Done when: New types exported; old interface preserved for backward compatibility during migration

- [ ] 2.2 Add `runAdaptersV2(projectRoot, constraints, context, config, adapterIds)` function that loads adapters from the registry by ID, calls `buildSharedContent`, and runs each adapter's `generate()`. Keep existing `runAdapters()` working unchanged.
  - Files: `src/adapters/base-adapter.ts`
  - Tests: Unit test with a mock adapter in registry produces expected output files. Unit test that unknown adapter IDs are skipped with warning. Unit test that idempotency check (skip if content unchanged) works.
  - Done when: New function works end-to-end with mock adapters; old function still works

## 3. Migrate existing adapters to new interface

- [ ] 3.1 Migrate `claude-md.ts` to `AdapterDefinition` format. Add `id: 'claude-code'`, `displayName`, `description`, `detect()` (checks `.claude/` or `CLAUDE.md`), and `generate()` returning `AdapterOutput[]` for CLAUDE.md. Use `SharedContent` for content.
  - Files: `src/adapters/claude-md.ts`
  - Tests: Unit test that `detect()` returns true when `.claude/` exists. Unit test that `generate()` output contains critical constraints. Unit test output is under 50 lines.
  - Done when: Adapter uses SharedContent; detection works; output unchanged from current

- [ ] 3.2 Migrate `agents-md.ts` to `AdapterDefinition` format as part of the `claude-code` adapter (generate returns both CLAUDE.md and AGENTS.md outputs).
  - Files: `src/adapters/claude-md.ts`, `src/adapters/agents-md.ts`
  - Tests: Unit test that claude-code adapter generates both CLAUDE.md and AGENTS.md entries.
  - Done when: AGENTS.md output bundled into claude-code adapter; standalone agents-md adapter kept for backward compat

- [ ] 3.3 Migrate `copilot-instructions.ts` to `AdapterDefinition` format. Add `id: 'copilot'`, detection (`.github/copilot-instructions.md` or `.github/` exists).
  - Files: `src/adapters/copilot-instructions.ts`
  - Tests: Unit test detection, unit test output format.
  - Done when: Adapter migrated; detection works

- [ ] 3.4 Migrate `cursor-rules.ts` to `AdapterDefinition` with format upgrade. Detect: `.cursor/` or `.cursorrules`. For new format, output `.cursor/rules/reins.mdc` with frontmatter: `---\ndescription: Project constraints generated by reins\nglobs: **\nalwaysApply: true\n---`. Fall back to `.cursorrules` when old file exists and `.cursor/rules/` doesn't.
  - Files: `src/adapters/cursor-rules.ts`
  - Tests: Unit test new format includes YAML frontmatter. Unit test fallback when `.cursorrules` exists. Unit test fresh project uses new path.
  - Done when: Both old and new format supported; detection works

- [ ] 3.5 Migrate `windsurf-rules.ts` to `AdapterDefinition` with format upgrade. Detect: `.windsurf/` or `.windsurfrules`. New format: `.windsurf/rules/reins.md`. Fall back to `.windsurfrules` when old file exists.
  - Files: `src/adapters/windsurf-rules.ts`
  - Tests: Unit test new format path. Unit test fallback. Unit test detection.
  - Done when: Both old and new format supported; detection works

## 4. Add new adapters

- [ ] 4.1 Create `src/adapters/cline.ts` — Cline / Roo Code adapter. `id: 'cline'`, output `.clinerules`, detect `.clinerules` exists. Format: standard Markdown with constraints grouped by severity.
  - Files: `src/adapters/cline.ts`
  - Tests: Unit test detection. Unit test output is valid Markdown with constraint groups.
  - Done when: Adapter registered; generates correct output

- [ ] 4.2 Create `src/adapters/continue-dev.ts` — Continue.dev adapter. `id: 'continue'`, output `.continue/rules/reins.md`, detect `.continue/` exists. Format: Markdown.
  - Files: `src/adapters/continue-dev.ts`
  - Tests: Unit test detection. Unit test output path creates parent directory.
  - Done when: Adapter registered; generates correct output

- [ ] 4.3 Create `src/adapters/amazon-q.ts` — Amazon Q adapter. `id: 'amazon-q'`, output `.amazonq/rules/reins.md`, detect `.amazonq/` exists. Format: Markdown.
  - Files: `src/adapters/amazon-q.ts`
  - Tests: Unit test detection. Unit test output.
  - Done when: Adapter registered; generates correct output

- [ ] 4.4 Create `src/adapters/augment.ts` — Augment Code adapter. `id: 'augment'`, output `.augment-guidelines`, detect `.augment-guidelines` or `.augment/` exists. Format: Markdown.
  - Files: `src/adapters/augment.ts`
  - Tests: Unit test detection. Unit test output.
  - Done when: Adapter registered; generates correct output

- [ ] 4.5 Create `src/adapters/aider.ts` — Aider adapter. `id: 'aider'`, output `CONVENTIONS.md`, detect `.aider.conf.yml` or `CONVENTIONS.md` exists. Format: Markdown with natural language guidelines.
  - Files: `src/adapters/aider.ts`
  - Tests: Unit test detection. Unit test output follows Aider conventions format.
  - Done when: Adapter registered; generates correct output

- [ ] 4.6 Create `src/adapters/gemini.ts` — Gemini CLI adapter. `id: 'gemini'`, output `GEMINI.md`, detect `GEMINI.md` exists. Format: Markdown.
  - Files: `src/adapters/gemini.ts`
  - Tests: Unit test detection. Unit test output.
  - Done when: Adapter registered; generates correct output

- [ ] 4.7 Register all new adapters in `ADAPTER_REGISTRY` in `src/adapters/index.ts`. Order: claude-code, cursor, copilot, windsurf, cline, continue, amazon-q, augment, aider, gemini.
  - Files: `src/adapters/index.ts`
  - Tests: Unit test that `ADAPTER_REGISTRY` has 10 entries with unique IDs.
  - Done when: All adapters in registry; `pnpm typecheck` passes

## 5. Interactive selection in init

- [ ] 5.1 Add `adapters` section to `ReinsConfig` in `src/state/config.ts`: `adapters: { enabled: string[] }`. Update `getDefaultConfig()` to include `adapters: { enabled: [] }`.
  - Files: `src/state/config.ts`
  - Tests: Unit test that `getDefaultConfig()` includes `adapters.enabled` as empty array.
  - Done when: Config type extended; default config includes adapters section

- [ ] 5.2 Implement `selectAdapters()` function in `src/commands/init.ts` that: (a) runs auto-detection on all registry adapters, (b) reads existing `config.adapters.enabled` if present, (c) in interactive mode shows multi-select with detected/saved items pre-checked, (d) in non-interactive mode uses detected or saved list, (e) returns final `string[]` of adapter IDs.
  - Files: `src/commands/init.ts`
  - Tests: Unit test non-interactive with no detection returns `['claude-code']`. Unit test non-interactive with `.cursor/` returns `['claude-code', 'cursor']`. Unit test `--adapters claude-code,cursor` bypasses detection and prompt.
  - Done when: Selection works in all three modes (interactive, non-interactive, explicit)

- [ ] 5.3 Wire `selectAdapters()` into `initCommand()`. Replace `runAdapters(projectRoot, constraints, context, config, DEFAULT_ADAPTERS)` with: `const adapterIds = await selectAdapters(...)` → `runAdaptersV2(projectRoot, constraints, context, config, adapterIds)`. Save selected IDs to `.reins/config.yaml`.
  - Files: `src/commands/init.ts`
  - Tests: Integration test that init with `--adapters claude-code` generates only CLAUDE.md and AGENTS.md, not .cursorrules. Unit test that selected adapters are persisted to config.
  - Done when: Init uses selection; config persisted; only selected files generated

- [ ] 5.4 Add `--adapters <ids>` option to CLI init command and `--no-input` flag.
  - Files: `src/cli.ts`
  - Tests: Unit test `--adapters claude-code,cursor` parses to correct array. Unit test `--no-input` sets noInput option.
  - Done when: CLI options work; help text updated

## 6. Update flow integration

- [ ] 6.1 Modify `reins update` in `src/commands/update.ts` to read `config.adapters.enabled` and regenerate only those adapters after constraint merge.
  - Files: `src/commands/update.ts`
  - Tests: Unit test that update only regenerates adapters listed in config. Unit test that update with empty config.adapters falls back to claude-code.
  - Done when: Update respects adapter selection; no re-prompting

## 7. Verification

- [ ] 7.1 Run `pnpm lint` and `pnpm test`. Add integration test: create a temp project, run `initCommand` with `--adapters claude-code,cline,aider`, verify only `CLAUDE.md`, `AGENTS.md`, `.claude/settings.json`, `.clinerules`, `CONVENTIONS.md` are generated and no other adapter outputs exist.
  - Files: `src/adapters/adapters.test.ts`
  - Tests: Integration test as described above. Existing adapter tests still pass.
  - Done when: All tests pass; only selected adapter files generated

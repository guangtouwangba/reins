## Tasks

- [ ] **Task 1: Define Adapter interface and runAdapters() orchestrator**
  - Description: Create `src/adapters/base-adapter.ts`. Define `Adapter` interface with `name: string`, `outputPath: string`, `generate(constraints: Constraint[], context: CodebaseContext): string`. Define `AdapterResult` type. Implement `runAdapters(projectRoot, constraints, context, adapters)`: for each adapter, call `generate()`, create parent directories with `fs.mkdirSync({ recursive: true })`, compare with existing file content, write only if changed, return `AdapterResult[]`.
  - Files: `src/adapters/base-adapter.ts`
  - Tests: Unit test with a mock adapter that returns `"hello"`; assert file is written to the correct path. Unit test idempotency: run twice with same output; assert file is written once (second call returns `skipped: true`).
  - Done when: Both unit tests pass; `pnpm typecheck` exits 0

- [ ] **Task 2: Implement cursor-rules.ts adapter**
  - Description: Create `src/adapters/cursor-rules.ts` implementing `Adapter`. `outputPath = '.cursorrules'`. `generate()`: write header `# {projectName} Development Rules\n\nGenerated by reins. Source: .reins/constraints.yaml\n`. Group constraints by severity (critical first, then important, then helpful). Each group under a `## {Severity}` heading. Each constraint as `- {rule}`. Return the full string.
  - Files: `src/adapters/cursor-rules.ts`
  - Tests: Unit test `generate()` with 2 critical and 1 helpful constraint; assert output string contains both severity headers and all three rule texts in correct order.
  - Done when: Output string is valid markdown; critical rules appear before helpful; `pnpm typecheck` exits 0

- [ ] **Task 3: Implement copilot-instructions.ts adapter**
  - Description: Create `src/adapters/copilot-instructions.ts` implementing `Adapter`. `outputPath = '.github/copilot-instructions.md'`. `generate()`: write a header with project name and stack, then three sections `## Critical Rules`, `## Important Rules`, `## Helpful Guidelines` each with bullet-point constraints. Append footer: `\n---\nFull constraint definitions: \`.reins/constraints.yaml\``. Return full string. Note: `runAdapters()` will create `.github/` directory automatically.
  - Files: `src/adapters/copilot-instructions.ts`
  - Tests: Unit test `generate()` with one constraint per severity level; assert all three section headers present; assert footer present.
  - Done when: Output matches GitHub Copilot instructions format; `pnpm typecheck` exits 0

- [ ] **Task 4: Implement windsurf-rules.ts adapter**
  - Description: Create `src/adapters/windsurf-rules.ts` implementing `Adapter`. `outputPath = '.windsurfrules'`. `generate()`: write header comment `# Windsurf Rules — generated by reins`. List all constraints flat (critical first) with severity tags: `[REQUIRED] {rule}` for critical, `[IMPORTANT] {rule}` for important, `[GUIDELINE] {rule}` for helpful. Return full string.
  - Files: `src/adapters/windsurf-rules.ts`
  - Tests: Unit test `generate()` with one of each severity; assert `[REQUIRED]`, `[IMPORTANT]`, `[GUIDELINE]` tags present; assert critical rules appear first.
  - Done when: Output matches expected format; severity tags correct; `pnpm typecheck` exits 0

- [ ] **Task 5: Implement claude-md.ts and agents-md.ts adapters**
  - Description: Create `src/adapters/claude-md.ts` implementing `Adapter`. `outputPath = 'CLAUDE.md'`. `generate()`: render critical constraints only — project name, stack one-liner, key commands, critical rules bullet list, Reins footer. Limit to 50 lines. Create `src/adapters/agents-md.ts` implementing `Adapter` with `outputPath = 'AGENTS.md'`. `generate()`: render global-scoped important constraints for the root directory. Add `generateAll(projectRoot, constraints, context)` method that iterates architecture-layer directories and writes per-directory `AGENTS.md` files.
  - Files: `src/adapters/claude-md.ts`, `src/adapters/agents-md.ts`
  - Tests: Unit test `claude-md.generate()` with 3 critical constraints; assert output ≤ 50 lines and contains all 3 rules. Unit test `agents-md.generate()` returns only important constraints.
  - Done when: Both adapters implemented; line limits enforced; `pnpm typecheck` exits 0

- [ ] **Task 6: Export DEFAULT_ADAPTERS from index.ts**
  - Description: Update `src/adapters/index.ts` to export `DEFAULT_ADAPTERS: Adapter[]` containing all five adapter instances (in order: claude-md, agents-md, cursor-rules, copilot-instructions, windsurf-rules). Also re-export `Adapter`, `AdapterResult`, and `runAdapters` from `base-adapter.ts`.
  - Files: `src/adapters/index.ts`
  - Tests: `import { DEFAULT_ADAPTERS } from './adapters/index.js'` compiles; assert `DEFAULT_ADAPTERS.length === 5`; assert each has a non-empty `name` and `outputPath`
  - Done when: All 5 adapters in the default set; `pnpm typecheck` exits 0
